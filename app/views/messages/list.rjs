if @messages != []
  page.insert_html :bottom, 'messages', :partial => 'list', :locals => {:messages => @messages}
  page << "ver = #{@messages.last.id};"

#page.assign :ver, @messages.last.id 


  page.replace_html 'last_message', @messages.last.id
  page.visual_effect  :highlight, "men_#{@messages.last.id}", 
                    :startcolor => "'#99ffcc'"
                    
  #page << "$('men_#{@messages.last.id}').focus;"
  #page[:men_10].toggle

end
