<h1>Novo Arquivos</h1>

<% form_for(:archive, :url => theme_archives_path) do |f| %>
  <p>
  <%= render :partial => 'form', :locals => {:f => f} %>

    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', theme_archives_path(@theme) %>
